# rcopy-gui
### Overview
A simple GUI for Robocopy written in C#. It aims to provide a user friendly interface for using Robocopy for copy or backup operations with large or many small files, which Explorer does not handle well.
